accountService nao conhece mais clientService

applicationService coordena o parsing de cpf -> clientId
serviços(exceto clientService) usam apenas clientId

applicationServiceTest

criação de StatementFormatter para tirar a responsabilidade de apresentação de uma transação de StatementData

money voltou a expor BigDecimal apenas para permitir apresentação de saldo formatada.